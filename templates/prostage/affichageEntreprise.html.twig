{% extends 'base.html.twig' %}

{% block contenu %}

<html>
    <body>
        <h1>Stage de {{entreprise.nom}}</h1>
        {% for stageCourant in entreprise.entreprises %} <!--problème de nom-->
        <li class = "list-group-item">
            <a href='{{path('prostageStages',{id:stageCourant.id})}}' class="text-decoration-none">
                {{stageCourant.titre}}
            </a>
        </li>
        {% endfor %}
    </body>
</html>

{% endblock %}