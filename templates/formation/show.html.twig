{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block contenu %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
            <tr>
                <th>Nom complet</th>
                <td>{{ formation.nomComplet }}</td>
            </tr>
        </tbody>
    </table>

    <button class="btn btn-dark" href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</button>
    <br><br>
    {{ include('formation/_delete_form.html.twig') }}
    <br>
    <a class="text-decoration-none" href="{{ path('formation_index') }}">Retour à la liste des formations</a>
{% endblock %}
